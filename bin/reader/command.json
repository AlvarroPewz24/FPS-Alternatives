command"haxeport" : $sudo port ;$game-engine; to ;haxe;
                    $sudo change ;3d-models; to ;png;
command"finddirectory" : $sudo check_running_apps, $app-name, ;checkdirectory;
                         $sudo close setup.exe
